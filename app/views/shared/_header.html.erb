<nav class="bg-gray-800 p-4 mb-16">
  <div class="container mx-auto flex justify-between items-center">
    <div>
      <a href="/" class="no-underline visited:text-white text-white text-lg font-semibold mr-5">Alexandria</a>
      <% if current_user.present? %>
        <a href="/dashboard" class="header-link">Dashboard</a>
        <a href="/users/edit" class="header-link">Profile</a>
      <% end %>
    </div>
    <div class="space-x-4">
    <% if current_user.present? %>
      <form method="DELETE" action="/users/sign_out">
        <button type="submit" class="link-button header-button gray-button">Sign Out</button>
      </form>
    <% else %>
      <a href="/users/sign_in" class="link-button header-button gray-button">Sign In</a>
    <% end %>
    </div>
  </div>
</nav>
